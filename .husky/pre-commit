# Format and lint staged files (via lint-staged)
# This runs ESLint --fix and Prettier on TypeScript files
# and Prettier only on JSON/Markdown files
npx lint-staged

# Run TypeScript type checking for the entire project (not limited to staged files)
echo "🔍 Running TypeScript type check for the entire project..."
npx tsc --noEmit || { echo "❌ TypeScript errors found in the project! Fix them before committing."; exit 1; }

echo "✅ Pre-commit checks passed!"
